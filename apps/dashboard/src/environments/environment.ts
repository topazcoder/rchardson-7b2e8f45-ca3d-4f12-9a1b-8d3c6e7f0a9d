export const environment = {
  production: false,
  // Local dev API URL. If using the Angular proxy, keeping '/api' is common.
  apiUrl: 'http://localhost:3000/api',
};

export const TOKEN_KEY = 'auth_token';
