/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{html,ts}'],
  darkMode: 'class', // use .dark on <html> or <body>
  theme: {
    extend: {},
  },
  plugins: [],
};
