# Example .env for the Nest API. Copy to `.env` and fill values.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=task_management

# Replace with a secure random value in production
JWT_SECRET=REPLACE_ME_WITH_A_STRONG_SECRET

PORT=3000
NODE_ENV=production
